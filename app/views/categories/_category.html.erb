<tr>
    <td><%= category.name %></td>
    <td class="text-right" style="width: 170px;">
        <div class="actions">
            <%= link_to  "Edit", edit_category_path(category), class: "glyphicon glyphicon-edit", remote: true %>
            <%= link_to  "Delete", category, class: "glyphicon glyphicon-trash", method: :delete, data: { confirm: "¿Estás seguro de eliminar esta Categoría?" } %>
    </td>
</tr>